node_modules
_site/
src/styles/styles.gen.css
.worktrees/

# Base JMdict : source du GÉNÉRATEUR de propositions de lecture, jamais livrée ni
# commitée (CC BY-SA 4.0 — on ne redistribue pas ses données, cf. tools/jmdict/).
.jmdict/
.kanjidic/

# Rapports d'arbitrage des énoncés — régénérés par tools/graph/audit-stems.mjs
data/enonces-arbitres.skel.json
docs/superpowers/enonces-a-arbitrer.md
docs/superpowers/mots-fabriques.md

# Réglages Claude Code propres à la machine (chemins absolus, allowlist locale)
.claude/settings.local.json
.superpowers/
kanji/*.pdf

# KanjiVG : tracés CC BY-SA 3.0, hors dépôt (cf. tools/kanjivg/).
.kanjivg/

# Sous-produits de `bun run cahier` et de son test : relevé des chapitres pour la
# seconde passe, et fichiers intermédiaires — ils doivent vivre sous la racine,
# `image()` et `json()` de Typst résolvant leurs chemins depuis elle.
kanji/.chapitres.json
kanji/.test-*
